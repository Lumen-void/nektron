# Blender Workspace

Generated `.blend` files are written to `3d/exports` by `3d/scripts/build_nektron_machine.py`.

Use Blender 4.x where possible because USD export support depends on the Blender build.
